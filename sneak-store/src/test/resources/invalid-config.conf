sneak {
    kafka {
        zookeeper {
            host="localhost"
            port="not number"
        }
        topic="sneak-store"
    }
}
cassandra {
    hosts=localhost
    keyspace=sneak_test
}